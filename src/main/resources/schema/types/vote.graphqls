type Vote {
    voteId: Int!
    voterId: Int!
    electionId: Int!
    firstChoiceCandidateId: Int
    secondChoiceCandidateId: Int
    thirdChoiceCandidateId: Int
    timestamp: DateTime
    ipAddress: String
    userAgent: String
}